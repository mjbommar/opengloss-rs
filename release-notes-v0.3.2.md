## Highlights
- **Homepage navigation**: `/` now prominently links to `/index`, so users can jump straight into the prefix browser from the landing page.
- **Markdown rendering fix**: lexeme entry text, aggregated definitions, and encyclopedia sections now output real HTML instead of escaped tags, keeping headings/lists intact.

## Packaging
- `scripts/package_release.sh v0.3.2`
- Upload `dist/opengloss-rs-v0.3.2-<target>.tar.zst` and its `.sha256` to the GitHub release.
